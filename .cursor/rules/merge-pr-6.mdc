---
description: One-shot — merge PR #6 on Ix, then delete this rule
alwaysApply: true
---

# Merge PR #6 on Ix

David: merge https://github.com/ixamal/ix/pull/6 into `main` on this Mac (`~/github/ixamal/ix`).

Branch: `cursor/ix-dcc-unreal-todo-bf77`. Notes-only `docs/TODO.md` — Floor DCC/Unreal pipeline + RedefineFX Chaos & Niagara Destruction learning session.

After merge: `git checkout main && git pull origin main`, then **delete this rule**.
